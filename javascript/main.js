function moClickJs(inputString) {
    console.log(inputString)
    // gradioApp().getElementById('mo_json_box').querySelector('textarea').value
    // const changeEvent = new Event('change', { bubbles: true });
//
// Dispatch the change event for the textarea
// textarea.dispatchEvent(changeEvent);

    // const event = new Event('input', { 'bubbles': true, "composed": true });
    // gradioApp().getElementById('mo_json_box').dispatchEvent(event);
    // textarea.dispatchEvent(event);

}
